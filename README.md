# splitwise-app-clone
clone of splitwise app

## How to Run This App

### Prerequisites
- Node.js installed (v16 or higher recommended)
- npm or yarn package manager
- Expo Go app on your mobile device (optional, for testing on physical device)

### Installation & Running

1. **Navigate to the project directory:**
   ```bash
   cd splitwise-app-clone
   ```

2. **Install dependencies:**
   ```bash
   npm install
   ```

3. **Start the development server:**
   ```bash
   npm start
   ```
   
   Or use specific platform commands:
   ```bash
   npm run android  # Run on Android emulator
   npm run ios      # Run on iOS simulator
   npm run web      # Run in web browser
   ```

4. **Open the app:**
   - Scan the QR code with Expo Go app (iOS/Android)
   - Press `a` for Android emulator
   - Press `i` for iOS simulator
   - Press `w` for web browser

### Test Credentials

For testing without Firebase setup:
- **Email:** `admin@admin.com`
- **Password:** `password`

### Additional Documentation

- `AUTHENTICATION_SETUP.md` - Firebase authentication setup guide
- `CREDENTIALS.md` - Test credentials and mock authentication details
